{
  "name": "twilight-rest-clone",
  "version": "1.0.0",
  "description": "A meditation and mindfulness website",
  "scripts": {
    "build": "npx tailwindcss -i styles.css -o main.css --minify",
    "dev": "npx tailwindcss -i styles.css -o main.css --watch",
    "start": "npx tailwindcss -i styles.css -o main.css --watch"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.1.11",
    "tailwindcss": "^4.1.11"
  }
}
